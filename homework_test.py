# Инициализируем переменную для хранения суммы
total = 0.0

# Бесконечный цикл для ввода чисел
while True:
    # Запрашиваем ввод у пользователя
   chr = input("Введите число (или любой другой символ для выхода): ")

    # Пытаемся преобразовать ввод в число (с плавающей точкой)
    if chr == float:
        # Если получилось, добавляем к сумме
        total += chr
   # Выводим итоговую сумму
        print(f"Сумма введённых чисел: {total}")

